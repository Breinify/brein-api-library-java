> 
```java
// Create the configuration object
final String secret = "time-rift";
final BreinConfig breinConfig = new BreinConfig(apiKey, secret);
>
// Set the configuration for later usage
Breinify.setConfig(breinConfig);
```
