<?xml version="1.0" encoding="UTF-8"?>

<project>
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.breinify</groupId>
    <artifactId>brein-api-library-java</artifactId>
    <version>1.1.1</version>

    <name>brein-api-library-java</name>
    <description>Brein API Library Java</description>
    <packaging>jar</packaging>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>http://www.opensource.org/licenses/mit-license.php</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <scm>
        <url>https://github.com/Breinify/brein-api-library-java</url>
        <connection>scm:git:git://github.com/Breinify/brein-api-library-java.git</connection>
        <developerConnection>scm:git:git@github.com:Breinify/brein-api-library-java.git</developerConnection>
    </scm>

    <developers>
        <developer>
            <id>marco.recchioni</id>
            <name>Marco Recchioni</name>
            <email>marco.recchioni@breinify.com</email>
            <organization>Breinify</organization>
        </developer>
        <developer>
            <id>philipp.meisen</id>
            <name>Philipp Meisen</name>
            <email>philipp.meisen@breinify.com</email>
            <organization>Breinify</organization>
        </developer>
    </developers>

    <url>https://www.breinify.com</url>

    <properties>
        <slf4jVersion>1.7.21</slf4jVersion>
        <unirestVersion>1.4.9</unirestVersion>
        <gsonVersion>2.7</gsonVersion>
        <jerseyClientVersion>1.19.1</jerseyClientVersion>
        <junitVersion>4.12</junitVersion>
    </properties>

    <dependencies>

        <!-- Logger Facade   -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4jVersion}</version>
        </dependency>

        <!-- can be activated to see some logging
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>${slf4jVersion}</version>
        </dependency>
         -->

        <!-- UNIREST -->
        <dependency>
            <groupId>com.mashape.unirest</groupId>
            <artifactId>unirest-java</artifactId>
            <version>${unirestVersion}</version>
            <optional>true</optional>
        </dependency>

        <!-- JSON Support -->
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>${gsonVersion}</version>
        </dependency>

        <!-- Jersey -->
        <dependency>
            <groupId>com.sun.jersey</groupId>
            <artifactId>jersey-client</artifactId>
            <version>${jerseyClientVersion}</version>
            <optional>true</optional>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junitVersion}</version>
            <scope>test</scope>
        </dependency>

    </dependencies>
</project>